west-commands:
  - file: scripts/generate_payload.py
    commands:
      - name: generate-payload
        class: GeneratePayload
        help: |
          invoke the Hart Software Services (HSS) payload generator
          to generate a payload suitable for flashing to the boards eMMC
